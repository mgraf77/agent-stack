# Policies

- `free-first.md` — no paid infrastructure required for normal use.
- `promotion-lifecycle.md` — how a catalogued source becomes a capability
  record and how a capability record gets added to a project profile.

These are referenced by `policies` arrays in `profiles/*.json`.
